By default the **Apply All** button is hidden for every user.

To let a specific user use it again, add them to the *Apply All Inventory
Adjustments* group (Settings > Users & Companies > Users, then enable the group
on the user).
