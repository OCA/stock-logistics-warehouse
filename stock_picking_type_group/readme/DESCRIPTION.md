This module allows to group operation types under a same object. Even if there is
the 'Type of Operation' that allows to qualify the type of operation (receipt, internal,...),
sometimes, you need to identify a group of operation types (like Pickings).
