This module allows to compute a reservation rate on picking levels that
depends on all pickings across a procurement group (same sale order, ...) and
for the same picking type group (e.g.: all PICK pickings).
