In future migrations, stock_move_actual_date and stock_valuation_layer_accounting_date
should be merged into one. Since stock_valuation_layer_accounting_date is already merged,
this bridge module has been created to retain the functionality.
