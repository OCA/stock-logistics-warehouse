<?xml version="1.0"?>
<openerp>
    <data>

        <record id="sale_order_view_form_inherit_sale_packaging" model="ir.ui.view">
            <field name="name">sale.order.form.sale.stock (sale_packaging)</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale_stock.view_order_form_inherit"/>
            <field name="arch" type="xml">
                <data>
                   <xpath expr="//page[@string='Order Lines']/field[@name='order_line']/form[@string='Sales Order Lines']//field[@name='product_uom']" position="attributes">
                        <attribute name="on_change">product_uom_change(parent.pricelist_id,product_id,product_uom_qty,product_uom,product_uos_qty,product_uos,name,parent.partner_id, False, False, parent.date_order, product_packaging, context)</attribute>
                        <attribute name="attrs">{'readonly' : [('product_packaging', '!=', False)]}</attribute>
                   </xpath>
                </data>
           </field>
        </record>

    </data>
</openerp>
