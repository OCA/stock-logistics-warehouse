<?xml version="1.0" encoding='UTF-8' ?>
<odoo>
    <record id="view_stock_quant_tree" model="ir.ui.view">
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.view_stock_quant_tree" />
        <field name="arch" type="xml">
            <xpath expr="//list" position="attributes">
                <attribute name="decoration-muted">
                    maturity_date &gt; current_date or quantity &lt; 0
                </attribute>
            </xpath>
            <xpath expr="//field[@name='quantity']" position="after">
                <field name="maturity_date" widget="remaining_days" />
            </xpath>
        </field>
    </record>

    <record id="view_stock_quant_tree_editable" model="ir.ui.view">
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.view_stock_quant_tree_editable" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='lot_id']" position="after">
                <field
                    name="maturity_date"
                    widget="remaining_days"
                    optional="hide"
                    column_invisible="context.get('hide_maturity_date')"
                />
            </xpath>
        </field>
    </record>

    <record id="view_stock_quant_tree_inventory_editable" model="ir.ui.view">
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.view_stock_quant_tree_inventory_editable" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='lot_id']" position="after">
                <field
                    name="maturity_date"
                    groups="stock.group_production_lot"
                    widget="remaining_days"
                    optional="hide"
                    column_invisible="context.get('hide_maturity_date')"
                />
            </xpath>
        </field>
    </record>

    <record id="quant_search_view_inherit_product_expiry" model="ir.ui.view">
        <field name="name">stock.quant.search.inherit</field>
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.quant_search_view" />
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='reserved']" position="after">
                <separator />
                <filter
                    string="Not mature products"
                    name="not_mature_products"
                    domain="[('maturity_date', '&gt;', context_today().strftime('%Y-%m-%d'))]"
                />
            </xpath>
        </field>
    </record>
</odoo>
