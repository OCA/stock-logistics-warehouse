<?xml version="1.0" encoding="UTF-8"?>
<openerp>
<data>

<record id="reception_checkpoint_tree" model="ir.ui.view">
    <field name="model">stock.move</field>
    <field name="arch" type="xml">
        <!-- <tree colors="grey:state == 'cancel';red:(state not in ('cancel','done')) and date &gt; current_date" string="Moves"> -->
        <tree colors="blue: visual_flag == 'toreceive'; orange: visual_flag == 'more'; red: visual_flag == 'inlate'">
        <!-- <tree> -->
            <!-- <field name="name"/> -->
            <field name="origin"/>
            <field name="visual_flag" invisible="1"/>
            <!-- <field name="create_date" invisible="1" groups="base.group_no_one"/> -->
            <field name="product_id" on_change="onchange_product_id(product_id,location_id,location_dest_id, False)"/>
            <field name="date_expected"/>
            <field name="received_date"/>
            <field name="product_uom_qty" on_change="onchange_quantity(product_id, product_uom_qty, product_uom, product_uos)"/>
            <field name="product_uom" string="Unit of Measure" groups="product.group_uom"/>
            <field name="ordered_qty" string="Ordered Qty"/>
            <field name="diff_qty"/>
            <field name="product_uos_qty" groups="product.group_uos"/>
            <field name="product_uos" groups="product.group_uos"/>
            <!-- <field name="location_id" groups="stock.group_locations"/> -->
            <!-- <field name="date" groups="base.group_no_one"/> -->
            <field name="state"/>
            <field name="location_dest_id" groups="stock.group_locations"/>
            <field name="picking_id" string="Reference"/>
            <field name="picking_type_id"/>
        </tree>
    </field>
</record>

<record id="reception_checkpoint_act" model="ir.actions.act_window">
    <field name="name">Stock Move Checkpoint</field>
    <field name="type">ir.actions.act_window</field>
    <field name="res_model">stock.move</field>
    <field name="view_mode">tree</field>
    <field name="domain">[("state", "!=", "done")]</field>
</record>


</data>
</openerp>
