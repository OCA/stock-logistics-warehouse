To activate this functionality, a **Route** must be configured in the product that contains a **Procurement Rule** (`stock.rule`) with the action set to **"Split Procurement."**

The module ensures that when this rule is found and there is partial stock for an MRP component, the splitting logic is automatically executed.
