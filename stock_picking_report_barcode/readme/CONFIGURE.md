The barcode is configured on the operation type via the
``stock_picking_form_barcode`` module. Once enabled, the barcode
automatically appears on the delivery slip report for pickings of
that operation type.
